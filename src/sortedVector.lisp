(in-package :jg-cl-sorted-vector)

(defclass sorted-vector ())
(defun make-sorted-vector ())

(defun lower-bound (vec element test))
(defun upper-bound (vec element test))
(defun equal-range (vec element test))
(defun binary_search (vec element test))

